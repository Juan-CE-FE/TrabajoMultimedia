<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionPeliculas.Pages.MainMenuPage"
             Title="Menú Principal"
             Padding="20">

    <VerticalStackLayout Spacing="16" VerticalOptions="CenterAndExpand">
        <Label Text="🎬 Gestión de Películas" 
               FontSize="24" 
               HorizontalOptions="Center" 
               FontAttributes="Bold"/>

        <Label Text="Selecciona una opción" 
               HorizontalOptions="Center" 
               TextColor="Gray"/>

        <BoxView HeightRequest="1" Color="#ccc" Margin="0,10"/>

        <!-- CRUD BÁSICO -->
        <Button Text="📋 Ver todas las películas" 
                Clicked="OnVerTodasClicked"
                BackgroundColor="#2196F3"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"/>

        <Button Text="➕ Crear película" 
                Clicked="OnCrearClicked"
                BackgroundColor="#4CAF50"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"/>

        <Button Text="🔍 Obtener película por Id" 
                Clicked="OnObtenerByIdClicked"
                BackgroundColor="#FF9800"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"/>

        <Button Text="✏️ Actualizar película" 
                Clicked="OnActualizarClicked"
                BackgroundColor="#9C27B0"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"/>

        <Button Text="🗑️ Eliminar película" 
                Clicked="OnEliminarClicked"
                BackgroundColor="#F44336"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"/>

        <BoxView HeightRequest="1" Color="#ccc" Margin="0,10"/>

        <!-- OPCIONES AVANZADAS - AQUÍ ESTÁN TUS ENDPOINTS -->
        <Button Text="🚀 Opciones Avanzadas (Exportar, Buscar, Posters)" 
                Clicked="OnOpcionesClicked"
                BackgroundColor="#673AB7"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"
                FontAttributes="Bold"/>
    </VerticalStackLayout>

</ContentPage>